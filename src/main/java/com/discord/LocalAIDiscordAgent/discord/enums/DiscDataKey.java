package com.discord.LocalAIDiscordAgent.discord.enums;

public enum DiscDataKey {
    GUILD_ID,
    CHANNEL_ID,
    USER_ID,
    USERNAME,
    USER_GLOBAL,
    SERVER_NICKNAME,
    CONVERSATION_ID,
    USER
}
